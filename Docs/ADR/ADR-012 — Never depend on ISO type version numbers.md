ADR-012 — Never depend on ISO type version numbers

Rule:

Message versions may change.
Type numbers may change.
XML element names are the stable business contract.
Providers operate on business semantics, not ISO internal type names.