ADR-011 – Architecture First, Implementation Second

It would state principles like:

Never extend architecture based on assumptions alone.
Every new architectural capability must have a proving test before implementation.
One completed architectural layer is worth more than three partially implemented ones.
Keep production code and experimental spikes separate.